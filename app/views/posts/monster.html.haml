= render 'layouts/header'
.detail-main
  .detail-main__total
    モンスター映画ランキング
  .detail-main__content
    - @monster.first(5).each.with_index(1) do |post, i|
      = link_to post_path(post.id) do
        .detail-main__content__each
          = i
          位👑
          = image_tag post.images[0][:pic]
          = post.title.truncate(11)
          %br
          .detail-main__content__each__favourite
            お気に入り数
            (
            = post.likes_count
            )